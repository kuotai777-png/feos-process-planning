import {solutions} from "../../mock/processPlan";
export function SolutionComparisonTable({selectedId="A",onSelect}:{selectedId?:string;onSelect?:(id:string)=>void}){const colors=["green","blue","orange","purple"];return <div className="solution-cards">{solutions.map((solution,index)=>{const id=String.fromCharCode(65+index);return <article onClick={()=>onSelect?.(id)} className={`solution-card ${colors[index]} ${selectedId===id?"selected":""}`} key={solution.title}><div className="solution-top"><b>方案{id}<span>{solution.title}</span></b><div><small>AI 推薦指數</small><strong>{"★".repeat(5-index)}<i>{"☆".repeat(index)}</i></strong></div></div>{[["成本影響",solution.cost],["時間影響",solution.time],["品質影響",solution.quality],["風險分析",solution.risk]].map(([label,value])=><div className="compare-row" key={label}><span>{label}</span><b>{value}</b></div>)}<div className="solution-desc"><b>方案說明</b><p>{solution.desc}</p></div>{selectedId===id&&<div className="solution-selected">✓ 已同步選擇</div>}</article>})}</div>}
