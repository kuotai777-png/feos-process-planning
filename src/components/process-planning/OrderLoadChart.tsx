import {ganttRows,orderSummary} from "../../mock/orders";
export function OrderLoadChart(){const dates=["07/21（一）","07/22（二）","07/23（三）","07/24（四）","07/25（五）","07/26（六）","07/27（日）"];return <><div className="card order-summary">{([["目前訂單總數",orderSummary.total],["進行中訂單",orderSummary.processing],["待生產訂單",orderSummary.waiting],["已排程訂單",orderSummary.scheduled]] as const).map(([k,v])=><div className="summary-item" key={k}><span>{k}</span><b>{v}<small className="muted"> 筆</small></b></div>)}</div><div className="card gantt-card"><b className="gantt-label">設備排程概覽（未來 7 天）</b><div className="gantt"><div className="gantt-head"><div style={{textAlign:"left"}}>設備</div>{dates.map(d=><div key={d}>{d}</div>)}<div>狀態</div></div>{ganttRows.map(r=><div className="gantt-row" key={r.name}><div className="gantt-name">{r.name}</div>{dates.map(d=><div className="gantt-cell" key={d}/>)}<div className={`gantt-bar ${r.tone}`} style={{gridColumn:`${r.start+1} / span ${r.span}`,gridRow:1}}>排程中</div></div>)}</div><div className="order-forecast"><div><span>預估可插單時間</span><b>07/24（四）下午</b></div><div><span>預估交期</span><b>2026/07/28</b></div><div><span>是否影響其他訂單</span><b className="warn">⚠ 輕微影響</b></div></div></div></>}
